/**
 * Seed entrypoint (requested by MVP onboarding).
 *
 * The project already has `prisma/seed/index.ts` which seeds realistic demo
 * data for all core domain tables. This file simply delegates so you can
 * run:
 *   npx tsx prisma/seed.ts
 */
import "./seed/index";

