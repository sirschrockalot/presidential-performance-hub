# Prisma

- **Schema:** `schema.prisma` (PostgreSQL)
- **Migrations:** `migrations/`
- **Seed:** `seed/index.ts` + `seed/steps/*` (run via `npm run prisma:seed`)

See **`docs/DATABASE.md`** for local setup, migrate, and seed commands.
